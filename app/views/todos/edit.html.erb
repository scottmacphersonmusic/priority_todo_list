<h3><%= @list.name %></h3>

<%= render partial: 'form', locals: { button_text: 'Update ToDo' } %>

<br>

<%= link_to('Delete ToDo', list_todo_path(@list, @todo),
            method: :delete, data: { confirm: 'Really?' },
            class: 'btn btn-danger') %>
